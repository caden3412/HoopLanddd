import fetch from 'node-fetch';

export async function scrapeInstagram(playerName: string): Promise<string> {
  // This requires an external 3rd party or proxy service; placeholder only
  console.warn('Instagram scraping requires proxy or official API');
  return '';
}
